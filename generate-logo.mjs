// This script was used to generate the logo using Google's AI.
// Since we've removed the @google/genai dependency, this script is now obsolete.
// To regenerate the logo, you would need to:
// 1. Re-add @google/genai to package.json
// 2. Get a valid API key for Google's Gemini API
// 3. Update the GoogleGenAI initialization with your API key
// 4. Run this script again

console.log('Logo generation script is disabled. The @google/genai dependency has been removed.');
console.log('To re-enable, restore the dependency and update this script with a valid API key.');
